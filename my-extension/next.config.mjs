/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Output static files
    distDir: 'out', // Output directory
    trailingSlash: true, // Ensure trailing slashes for paths
  };
  
  export default nextConfig;